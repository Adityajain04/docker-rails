version: '3'
services:
  db:
    image: mysql
    ports:
      - "3001:3001"

  web:
    build: .
    command: bin/rails server --port 3000 --binding 0.0.0.0
    ports:
      - "3000:3000"
    links:
      - db
    volumes:
      - .:/docker-rails